apiVersion: v1
kind: Service
metadata:
  name: mincepierank-sveltekit
spec:
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: mincepierank-sveltekit
  type: ClusterIP
